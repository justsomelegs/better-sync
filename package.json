{
  "name": "ts-sync-engine",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript-only sync engine with bring-your-own-database adapters (InMemory, SQLite/libSQL, Postgres)",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "example": "tsx examples/basic.ts"
  },
  "devDependencies": {
    "@types/node": "^22.7.7",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}

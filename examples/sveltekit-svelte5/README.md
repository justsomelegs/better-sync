# SvelteKit (Svelte 5) example for just-sync

## Run locally

```bash
# from repo root
npm run build
npm pack
cd examples/sveltekit-svelte5
npm install
npm run dev
```

Then open http://localhost:5173.